# Void — 언리얼 마스터 과제 제출

> 필수: 샷건 + 총 반동 + 무기별 커스터마이징
> 도전: 적 AI 비동기 Trace 가시선 검증

---

## 1. 필수 과제 — 샷건 + 반동 + 커스터마이징

### 1-1. 무기 파라미터 DataAsset (총마다 커스터마이징)

`UVOIDWeaponConfig`를 `UDataAsset` 기반으로 만들어 무기별 인스턴스(`DA_Rifle`, `DA_Shotgun`)를 따로 둔다. 발사·반동·소음·FX 모두 데이터로 분리되어 코드 수정 없이 신규 무기 추가 가능.

- `Source/VoidUnreal/Public/Weapon/VOIDWeaponConfig.h`
  - `EVOIDWeaponClass` (Rifle / Shotgun / Pistol) — `VOIDWeaponConfig.h:11`
  - `PelletCount` 펠릿 개수 (샷건=8, 라이플=1) — `VOIDWeaponConfig.h:35`
  - `SpreadDegrees` 산탄 각도 — `VOIDWeaponConfig.h:39`
  - `Range` / `DamagePerPellet` / `FireInterval` — `VOIDWeaponConfig.h:43,47,51`
  - `RecoilPitch` / `RecoilYawJitter` / `RecoilRecoverPerSec` — `VOIDWeaponConfig.h:55,59,63`
  - `NoiseRadius`, `FireSound`, `CameraShakeClass` — `VOIDWeaponConfig.h:67,71,75`

### 1-2. 무기 시스템 캡슐화 — `UVOIDWeaponComponent`

발사·산탄·반동 누적/회복·장착을 `UActorComponent`로 캡슐화. Pawn은 입력 → 컴포넌트 위임만 담당.

- `Source/VoidUnreal/Public/Components/VOIDWeaponComponent.h`
  - `EquipWeapon` / `TryFire` / `TickRecoil` / `OnWeaponEquipped` / `OnWeaponFired` — `VOIDWeaponComponent.h:24-46`

- `Source/VoidUnreal/Private/Components/VOIDWeaponComponent.cpp`
  - **샷건 펠릿 산탄 루프** — `VOIDWeaponComponent.cpp:67-82`
    - 펠릿 수만큼 `FMath::VRandCone(Forward, SpreadRad)`로 무작위 산탄 방향
  - **단일 펠릿 트레이스 + 데미지** — `VOIDWeaponComponent.cpp:100-129` (`FireSinglePellet`)
  - **반동 누적** — `VOIDWeaponComponent.cpp:84-89`
    - `PendingRecoilPitch += Weapon->RecoilPitch`
    - `PendingRecoilYaw += FRandRange(-Jitter, +Jitter)` — 좌우 불규칙
  - **반동 회복 (FInterpTo)** — `VOIDWeaponComponent.cpp:160-175` (`TickRecoil`)
    - 매 틱 누적값을 컨트롤러에 적용 후 `RecoilRecoverPerSec` 속도로 0으로 보간
  - **데미지 분기 (캐릭터 vs 일반 액터)** — `VOIDWeaponComponent.cpp:131-158` (`ApplyHitDamage`)

### 1-3. 플레이어 통합 — Fire / Recoil / 무기 스왑

- `Source/VoidUnreal/Public/Characters/VOIDPlayerCharacter.h`
  - `WeaponComp` 컴포넌트 보유 — `VOIDPlayerCharacter.h:86`
  - `RifleConfig` / `ShotgunConfig` 두 무기 슬롯 — `VOIDPlayerCharacter.h:117-121`
  - `SwitchToRifleAction` / `SwitchToShotgunAction` 입력 (1/2 키) — `VOIDPlayerCharacter.h:111-115`

- `Source/VoidUnreal/Private/Characters/VOIDPlayerCharacter.cpp`
  - **WeaponComp 생성** — `VOIDPlayerCharacter.cpp:63`
  - **반동 컨트롤러 입력 적용** — `VOIDPlayerCharacter.cpp:93-99`
    - 매 틱 `WeaponComp->TickRecoil` → `AddControllerPitchInput` / `AddControllerYawInput`
  - **1/2 키 무기 스왑 바인딩** — `VOIDPlayerCharacter.cpp:118-119`
  - **OnSwitchToRifle / OnSwitchToShotgun** — `VOIDPlayerCharacter.cpp:122-130`
  - **Fire 입력** — `VOIDPlayerCharacter.cpp:237-253`
    - 카메라 위치/회전을 머즐로 전달 → `WeaponComp->TryFire`
    - 발사 성공 시 무게 비례 소음 방출
  - **EquipWeapon BP 래퍼** — `VOIDPlayerCharacter.cpp:256-262`

### 1-4. 데이터 자산 (에디터)

- `Content/Weapons/DA_Rifle.uasset` — `WeaponClass=Rifle`, `PelletCount=1`, `SpreadDegrees=1.5`, `RecoilPitch=1.2`
- `Content/Weapons/DA_Shotgun.uasset` — `WeaponClass=Shotgun`, `PelletCount=8`, `SpreadDegrees=8`, `RecoilPitch=4.0`, `FireInterval=0.7`

---

## 2. 도전 과제 — 적 AI 비동기 Trace 가시선

### 2-1. 핵심 구현

좀비 AIController의 Tick에서 일정 주기마다 `AsyncLineTraceByChannel`로 비동기 가시선 트레이스를 발사. 콜백(`HandleAsyncTrace`)에서 블랙보드 `TargetActor` / `bHasTarget`를 갱신해 BehaviorTree가 추격으로 분기한다. 게임 스레드 블로킹 없음.

- `Source/VoidUnreal/Public/AI/VOIDZombieAIController.h`
  - `bUseAsyncSight` / `AsyncTraceInterval` / `SightRange` 옵션 — `VOIDZombieAIController.h:46-53`
  - `FTraceDelegate AsyncTraceDelegate` — `VOIDZombieAIController.h:56`
  - `RequestAsyncSight()` / `HandleAsyncTrace()` 선언 — `VOIDZombieAIController.h:59-60`

- `Source/VoidUnreal/Private/AI/VOIDZombieAIController.cpp`
  - **Tick 주기 호출 (델리게이트 1회 바인드 후 간격 체크)** — `VOIDZombieAIController.cpp:57-72`
  - **비동기 트레이스 요청** — `VOIDZombieAIController.cpp:117-138` (`RequestAsyncSight`)
    - `GetWorld()->AsyncLineTraceByChannel(EAsyncTraceType::Single, ..., &AsyncTraceDelegate)`
    - SightRange 밖이면 조기 컷
  - **콜백 — 블랙보드 갱신** — `VOIDZombieAIController.cpp:140-154` (`HandleAsyncTrace`)
    - `Data.OutHits[0].bBlockingHit == false` → 가시선 확보 → `TargetActor` / `bHasTarget` 세팅

### 2-2. 청각(AIPerception)과의 역할 분담

- AIPerception Hearing — 발사음·픽업음 등 노이즈 방향 감지 (`VOIDZombieAIController.cpp:33-34`, `OnPerceptionUpdated`)
- 비동기 Trace — 노이즈가 없어도 시야 안에 들어왔는지 주기 점검
- 두 시스템 모두 같은 블랙보드(`TargetActor`)를 공유해 BT가 일관된 추격 로직 수행

---

## 3. 디렉토리 구조

```
Source/VoidUnreal/
├── Public/
│   ├── Weapon/VOIDWeaponConfig.h          ← 필수: 무기 커스터마이징 DataAsset
│   ├── Components/VOIDWeaponComponent.h   ← 필수: 발사/반동/장착 캡슐화
│   ├── Characters/VOIDPlayerCharacter.h   ← 필수: 무기 슬롯 + 1/2 키 스왑
│   └── AI/VOIDZombieAIController.h        ← 도전: 비동기 Trace
└── Private/
    ├── Weapon/VOIDWeaponConfig.cpp
    ├── Components/VOIDWeaponComponent.cpp ← 펠릿 루프 + 반동 회복
    ├── Characters/VOIDPlayerCharacter.cpp ← TickRecoil 적용 + Fire 라우팅
    └── AI/VOIDZombieAIController.cpp      ← AsyncLineTraceByChannel + 콜백
```

---

## 4. 검증 체크리스트

### 필수
- [ ] 1 키 → 라이플 장착 (단발, 약한 반동)
- [ ] 2 키 → 샷건 장착 (펠릿 8발 산탄, 강한 반동)
- [ ] 발사 시 카메라 Pitch가 위로 튐 → `RecoilRecoverPerSec` 속도로 복귀
- [ ] DA_Rifle / DA_Shotgun의 파라미터를 에디터에서 변경 → 컴파일 없이 즉시 반영

### 도전
- - VoidUnreal/Content/VoidUnreal/AI/BP_VoidZombieAIController.uasset
- [ ] 좀비 컨트롤러 BP에서 `bUseAsyncSight=true` 설정
- [ ] 좀비가 벽 너머는 추격 안 함, 시야 트인 곳에서만 반응
- - VoidUnreal/Content/VoidUnreal/Maps/Lv_ZombieTest.umap 레벨에서 확인가능
- [ ] `stat unit` 으로 게임 스레드 정체 없음 확인 (비동기이므로 동기 트레이스 대비 부하 분산)
